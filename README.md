# Projet Bourse — version spectateur

Copie publiée automatiquement depuis le PC du propriétaire (scripts/publier_en_ligne.py).
Ne pas modifier ici : chaque publication écrase ce dépôt.

Streamlit Cloud : fichier principal `src/bourse/ui/app.py`, Python 3.12.
Les données arrivent dans la branche `donnees` (fichier donnees.zip), réécrite à chaque publication.
